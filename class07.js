const myFunc = (ins, ta, ...student) => {
    return student;
}

const echo = (text) => text;
const sayHi = (greeting, name) => `${greeting}, ${name}`;
const getClass = () => `INT201`;

console.log(getClass());